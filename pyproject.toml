[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sol-plex-problems"
version = "0.1.0"
description = "A comprehensive toolkit for problem-solving"
